void main(){
    // int int_name = +42;
    char chr_identifier = 'ascii';
    char buff[30] = "String \\ containing \" \'escape sequences\' \" ! \n\t \? ";
    char arr[4] = ""; 
    char* chr_pointer = &chr_identifier;
    int x;
    int i = x;
    int n = 7; 
    int res = 0;
    for(i = 1; i <= n; i = i + 1){
        int _res = res*i ;
        res = _res ;
    }

    object->this;

    // dummy operations
    if( res < 234 ){
        res = res - 1 ;
    } else {
        res = res + 1 ;
    }

    printStr( "print:" ); // print statements
    printInt(res);
    printStr("\n");

    return 0;
}
